# Airflow (Linux/macOS only — leave default on Windows)
AIRFLOW_UID=50000

# AQI API key — never hardcode this in scripts
AQI_API_KEY=your_api_key_here

# Neon Postgres connection string (warehouse)
NEON_DB_URL=postgresql://user:password@host/dbname?sslmode=require